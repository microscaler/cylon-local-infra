---
# RETIRED — use playbooks/provision_sparks.yml (canonical).
#
#   just spark-provision --tags hf_prefetch,spark_assert
#
# hf_prefetch inner tasks propagate tags via spark_provision `apply:` blocks.

- name: Retired playbook — redirect to provision_sparks.yml
  hosts: localhost
  gather_facts: false
  tasks:
    - name: Stop — refresh_hf_prefetch.yml is retired
      ansible.builtin.fail:
        msg: >-
          playbooks/refresh_hf_prefetch.yml is retired. Use:
            just spark-provision --tags hf_prefetch,spark_assert
