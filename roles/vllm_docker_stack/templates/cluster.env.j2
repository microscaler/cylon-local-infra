# Managed by Ansible (vllm_docker_stack). Do not edit on host — change group_vars and re-run playbook.
# Upstream: spark-vllm-docker .env

CLUSTER_NODES={{ vllm_docker_cluster_nodes_csv }}
{% if vllm_docker_copy_hosts_csv | default('') | length > 0 %}
COPY_HOSTS={{ vllm_docker_copy_hosts_csv }}
{% endif %}
ETH_IF={{ vllm_docker_eth_if_effective }}
IB_IF={{ vllm_docker_ib_if }}
MASTER_PORT={{ vllm_docker_master_port }}
CONTAINER_NAME={{ vllm_docker_container_name }}
LOCAL_IP={{ vllm_docker_leader_interconnect_ip }}
